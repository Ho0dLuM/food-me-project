{% set cuisines = [ 'american', 'italian', 'mexican', 'thai' ] %}
{% for cuisine in cuisines %}
  <option value="{{ loop.index }}"
    {% if restaurant.cuisine_type == loop.index %}
      selected="selected"
    {% endif %}>
    {{ cuisine | capitalize }}
  </option>
{% endfor %}
