{% extends "../_base.njk" %}

{% block title %}{% endblock %}

{% block content %}

  <div class="container">
    <h1>{{title}}</h1>
    {% for restaurant in restaurants %}
      <p>{{ restaurant | dump }}</p>
    {% endfor %}
  </div>

{% endblock %}
